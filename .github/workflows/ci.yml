name: CI

on: [push]

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run tests with coverage
        run: dotnet test --no-build --configuration Release --collect:"XPlat Code Coverage" --results-directory ./coverage

      - name: Verify sample outputs
        shell: bash
        run: |
          if [ -f "samples/Scarlet.Bun.Sample/wwwroot/js/bundle.min.js" ]; then
            echo "✓ JavaScript bundle created successfully"
            ls -lh samples/Scarlet.Bun.Sample/wwwroot/js/bundle.min.js
          else
            echo "⚠ JavaScript bundle not found (expected due to Bun CI issues)"
          fi
          if [ -f "samples/Scarlet.Bun.Sample/wwwroot/css/style.min.css" ]; then
            echo "✓ CSS bundle created successfully"
            ls -lh samples/Scarlet.Bun.Sample/wwwroot/css/style.min.css
          else
            echo "⚠ CSS bundle not found (expected due to Bun CI issues)"
          fi

      - name: Pack NuGet packages
        shell: bash
        run: |
          # Use 0.0.1-ci for testing
          VERSION="0.0.1-ci.${{ github.run_number }}"
          echo "Packing with version: $VERSION"
          
          # Pack the main MSBuild package
          dotnet pack src/Scarlet.Bun.MSBuild/Scarlet.Bun.MSBuild.csproj \
            --no-build \
            --configuration Release \
            --output ./packages -p:Version=$VERSION
          
          # Pack all runtime packages (without --no-build because they need to restore and copy binaries)
          dotnet pack src/Scarlet.Bun.Runtime.windows-x64-baseline/Scarlet.Bun.Runtime.windows-x64-baseline.csproj \
            --configuration Release \
            --output ./packages -p:Version=$VERSION
          
          dotnet pack src/Scarlet.Bun.Runtime.linux-x64-baseline/Scarlet.Bun.Runtime.linux-x64-baseline.csproj \
            --configuration Release \
            --output ./packages -p:Version=$VERSION
          
          dotnet pack src/Scarlet.Bun.Runtime.linux-aarch64/Scarlet.Bun.Runtime.linux-aarch64.csproj \
            --configuration Release \
            --output ./packages -p:Version=$VERSION
          
          dotnet pack src/Scarlet.Bun.Runtime.darwin-x64-baseline/Scarlet.Bun.Runtime.darwin-x64-baseline.csproj \
            --configuration Release \
            --output ./packages -p:Version=$VERSION
          
          dotnet pack src/Scarlet.Bun.Runtime.darwin-aarch64/Scarlet.Bun.Runtime.darwin-aarch64.csproj \
            --configuration Release \
            --output ./packages -p:Version=$VERSION
          
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: List generated packages
        shell: bash
        run: ls -lh ./packages/

      - name: E2E Test - Install packages and create test project
        shell: bash
        run: |
          chmod +x tests/e2e/verify-package-installation.sh
          tests/e2e/verify-package-installation.sh "${{ github.workspace }}" "${{ env.VERSION }}"

      - name: E2E Test - Verify Bun execution
        shell: bash
        run: |
          # The test directory will be in /tmp/nuget-verification-* (created by previous step)
          # Find the most recent test directory
          TEST_DIR=$(ls -td /tmp/nuget-verification-* 2>/dev/null | head -1)
          
          if [ -z "$TEST_DIR" ]; then
            echo "⚠ Test directory not found - E2E test may have cleaned up early"
            exit 0
          fi
          
          cd "$TEST_DIR/TestBunPackage"
          
          # Verify that the build.mjs created the output file
          if [ -f "output.txt" ]; then
            echo "✓ Bun executed successfully via NuGet package!"
            cat output.txt
          else
            echo "⚠ Bun execution failed - output.txt not found (may be expected in CI)"
            # Don't fail CI for this - Bun may not work in all CI environments
          fi
          
          echo "✓ E2E package verification completed!"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: ./coverage
          fail_ci_if_error: false
          flags: ${{ matrix.os }}
          name: codecov-${{ matrix.os }}
