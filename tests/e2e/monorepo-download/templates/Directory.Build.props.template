<Project>
  <PropertyGroup>
    <!-- Shared Bun download configuration for all projects in the monorepo -->
    <BunRuntimeDownload>true</BunRuntimeDownload>
    <BunVersionDownload>{{BUN_VERSION}}</BunVersionDownload>
    <BunRuntimeDirectory>{{SHARED_RUNTIME_DIR}}</BunRuntimeDirectory>
  </PropertyGroup>
</Project>
