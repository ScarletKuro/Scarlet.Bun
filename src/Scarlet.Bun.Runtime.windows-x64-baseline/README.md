# Scarlet.Bun.Runtime.windows-x64-baseline

This package contains the Bun runtime for Windows x64 (baseline). It is a dependency of the Scarlet.Bun.MSBuild package and should not be installed directly.
